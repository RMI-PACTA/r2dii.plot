#' @param convert_label A symbol. The unquoted name of a function to apply to
#'   y-axis labels. For example:
#'   * To convert labels to uppercase use `convert_label = toupper`.
#'   * To get the default behavior of `<%= fun%>()` use
#'   `convert_label = <%= value%>`.
